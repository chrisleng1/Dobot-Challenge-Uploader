# Dobot-Challenge-Uploader

A minimal web application for uploading large Docker images to Aliyun OSS.

## Setup

1. Install dependencies: `npm install`
2. Configure OSS credentials in `server.js`
3. Run the server: `npm start`

## Usage

Open `http://localhost:3000` in your browser, select a file, and upload.
